<Page x:Class="Sanatory.View.Information"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Sanatory.View"
      xmlns:vm="clr-namespace:Sanatory.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Information">

    <Page.Resources>
        <vm:InformationVM x:Key="infVM"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content=" "/>
        <Label Grid.Row="1" Content="Выберите дату" FontSize="16"/>
        <Calendar Grid.Row="2"/>
        <Label Grid.Column="1" Grid.Row="1" Content="Мероприятия" FontSize="16"/>
        <ListView Grid.Row="2" Grid.Column="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название"/>
                    <GridViewColumn Header="Место"/>
                    <GridViewColumn Header="Время"/>
                    <GridViewColumn Header="Дата"/>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid> 
</Page>
