<Page x:Class="Sanatory.View.ResourcePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="900"
      Title="Материалы">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="📦 Материалы сотрудника" 
                   Style="{StaticResource Label}"
                   FontSize="16" FontWeight="Bold"/>
            <TextBlock Text=" (введите количество и нажмите «Использовать»)" 
                       Style="{StaticResource TextBlock}"
                       VerticalAlignment="Center"
                       FontSize="11" FontStyle="Italic"/>
        </StackPanel>

        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Resources}" 
                  SelectedItem="{Binding SelectedResource}"
                  Style="{StaticResource ListView}">
            <ListView.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="Foreground" Value="#1B5E20"/>
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" 
                                    DisplayMemberBinding="{Binding Title}" 
                                    Width="300"
                                    HeaderContainerStyle="{DynamicResource ListViewColumnHeader}"/>
                    <GridViewColumn Header="Доступно" 
                                    DisplayMemberBinding="{Binding Amount, StringFormat={}{0:N2}}" 
                                    Width="120"
                                    HeaderContainerStyle="{DynamicResource ListViewColumnHeader}"/>
                    <GridViewColumn Header="Использовать" Width="250"
                                    HeaderContainerStyle="{DynamicResource ListViewColumnHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding UseAmount, UpdateSourceTrigger=PropertyChanged}" 
                                             Width="80" 
                                             Margin="0,0,5,0"
                                             VerticalAlignment="Center"
                                             Style="{StaticResource TextBox}"/>
                                    <Button Content="Использовать" 
                                    Command="{Binding DataContext.UseResourceCommand, 
                                                RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource Button}"
                                    Padding="10,3"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>