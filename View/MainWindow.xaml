<Window x:Class="Sanatory.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sanatory.View" 
        xmlns:vm="clr-namespace:Sanatory.ViewModel"
        mc:Ignorable="d"
        DataContext="{DynamicResource vm}"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <vm:MainWindowVM x:Key="vm"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="Минеральные источники" Grid.ColumnSpan="2" FontSize="24"/>
        <Menu Grid.Column="3">
            <MenuItem Header="Админ" FontSize="20"/>
        </Menu>
        <Frame NavigationUIVisibility="Hidden" Content="{Binding Pages}" Grid.Column="3"/>

    </Grid>
</Window>
