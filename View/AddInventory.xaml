<Page x:Class="Sanatory.View.AddInventory"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Sanatory.View"
      xmlns:vm="clr-namespace:Sanatory.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{DynamicResource invAddVM}"
      Title="AddInventory" Style="{DynamicResource Page}">

    <Page.Resources>
        <vm:InvAddVM x:Key="invAddVM"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Content=" "/>
        <StackPanel Grid.Row="1">
            <Label Content="Введите название материала" Style="{DynamicResource Label}"/>
            <TextBox Style="{DynamicResource TextBox}"/>
            <Label Content="Выберите количество материала" Style="{DynamicResource Label}"/>
            <TextBox Style="{DynamicResource TextBox}"/>
            <Label Content="Выберите сотрудника, которому выдать этот материал" Style="{DynamicResource Label}"/>
            <ComboBox Style="{DynamicResource ComboBox}"/>
            <Button Content="Сохранить" Style="{DynamicResource Button}"/>
        </StackPanel>
    </Grid>
</Page>
