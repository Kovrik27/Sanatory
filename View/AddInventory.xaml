<Page x:Class="Sanatory.View.AddInventory"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Sanatory.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddInventory" Style="{DynamicResource Page}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Content=" "/>
        <StackPanel Grid.Row="1">
            <Label Content="Выберите материал" Style="{DynamicResource Label}"/>
            <ComboBox Style="{DynamicResource ComboBox}"/>
            <Label Content="Выберите количество" Style="{DynamicResource Label}"/>
            <ComboBox Style="{DynamicResource ComboBox}"/>
            <Button Content="Отправить заявку" Style="{DynamicResource Button}"/>
        </StackPanel>
    </Grid>
</Page>
